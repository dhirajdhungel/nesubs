
  # Mobile Marketplace App Design (Copy)

  This is a code bundle for a Mobile Marketplace app design. The original visual mockups were created with a design tool and are not required to run this project.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  